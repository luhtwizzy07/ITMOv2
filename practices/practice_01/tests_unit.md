# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| SEC-1 | Санитайзинг токенов | diff с токеном `x-token:abc123` | Секрет заменён на `[REDACTED]`; исходный diff не мутирован | SEC-1 (context.md); ADR «Решение» |
| SEC-1 | Санитайзинг password | diff с `password=letmein` | `letmein` → `[REDACTED]`; остальной контекст сохранён | SEC-1 (context.md) |
| SEC-1 | Санитайзинг private key | diff с фрагментом `-----BEGIN PRIVATE KEY----- ...` | Тело ключа замаскировано; маркеры границ сохранены | SEC-1 (context.md) |
| SEC-1 | Идемпотентность санитайзинга | уже замаскированный diff | Повторная обработка не меняет текст | SEC-1; устойчивость пайплайна |
| SEC-1 | Ложные срабатывания | безопасные подстроки, похожие на секреты | Не маскируются; маскируется только реальный секрет | SEC-1; негативные кейсы |
| API-1 | Лимит длины: допустимый | строка длиной 20000 | Проходит проверку | API-1 (context.md) |
| API-1 | Лимит длины: превышение | строка длиной 20001 | Генерируется ошибка лимита до вызова LLM | API-1; ADR «Решение» |
| API-1 | Пустая строка | `""` | Ошибка валидации (min_length=1) | Контракт входа (context.md); решение команды: min_length=1 |
| OUT-1 | Структура ответа | результат сборки ответа | Объект содержит `summary:str`, `risks:list`, `checks:list[str]` | OUT-1 (context.md); ADR «Решение» |
| OUT-1 | Ограничение risks ≤ 3 | входные риски длиной 5 | Итоговое `risks` обрезано до 3 | OUT-1 (context.md) |
| OUT-1 | Валидация структуры risk | один risk без поля `evidence` | Ошибка/отклонение структуры | OUT-1 (обязательные поля risk) |
| REL-1 | Таймаут адаптера | заглушка LLM блокирует >10s | Бросается TimeoutError адаптера | REL-1; ADR «Интерфейс/таймаут 10s» |
| REL-1 | Retry x3 с jitter | заглушка кидает транзиентную ошибку 2 раза, затем ок | Совершается 3 попытки; суммарное время учитывает jitter | ADR «Решение» (ретраи x3 с jitter) |
| REL-1 | Детеминизм jitter | подменённый random/clock | Jitter в допустимом диапазоне; тест детерминирован | Надёжность тестов; ретраи |
| REL-1 | Классификация ошибок: timeout | TimeoutError из источника | Класс `timeout` | ADR «Маппинг ошибок LLM → HTTP» (классы) |
| REL-1 | Классификация ошибок: network/5xx | исключение сети/5xx | Класс `unavailable` | ADR «Маппинг ошибок LLM → HTTP» |
| REL-1 | Классификация ошибок: internal | произвольное исключение | Класс `internal` | ADR «Маппинг ошибок LLM → HTTP» |
| Адаптер | Сигнатура generate | вызов `generate(input: str, timeout_s: int=10)` | Возвращает dict с ключами `summary, risks, checks` | ADR «Интерфейс LLM-адаптера» |
| Адаптер | Async-инвариант | `await generate(...)` | Не блокирует event loop; ошибки пробрасываются ожидаемо | ADR «Решение (async)» |
| Маппинг ошибок | Перевод внутренних классов ошибок в сервисные типы | `timeout/unavailable/internal` | Сообщения: `"LLM timeout"/"LLM unavailable"/"Internal error"` | ADR «Маппинг ошибок LLM → HTTP» (без HTTP, слой сервисных ошибок) |
| OBS-1 | Политика логирования: положительный | лог-хелпер вызывается c request_id, duration, status | В лог попадают только эти поля | OBS-1 (context.md); ADR «Политика логирования» |
| OBS-1 | Политика логирования: негативный | попытка залогировать diff/prompt/response | Эти поля не записываются/редактируются | OBS-1 (context.md) |

## Как использовали AI

- Для чего: Определить изолированные проверки правил SEC-1, API-1, REL-1
- Тип промпта: master prompt
- Строка в [`prompts.md`](prompts.md): P1-02
- Что проверили и исправили сами: Добавили проверки OUT-1 и OBS-1; уточнили интерфейс адаптера, ретраи/jitter, классификацию и маппинг ошибок; зафиксировали граничные случаи по API-1
