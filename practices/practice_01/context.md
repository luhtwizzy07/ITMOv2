# Контекст AS IS и Context Pack

## Продукт и команда сейчас

| Вопрос | Ответ | Источник или допущение |
|---|---|---|
| Какой продукт или сервис рассматриваем? | Сервис ревью PR по diff через внешний LLM (FastAPI) | TRAINING_PR.diff (app/api.py, app/review_service.py) |
| Кто им пользуется? | Клиент API, отправляющий diff на ревью | Следует из POST /api/reviews |
| Как устроен текущий процесс? | Клиент шлёт JSON с ключом diff → API передает строку diff в ReviewService → Service формирует prompt и вызывает llm.generate → возвращает словарь с полем comment | TRAINING_PR.diff строки: app/api.py:35-38; app/review_service.py:19-22 |
| Где возникает задержка или ошибка? | Нет валидации и лимитов на размер diff; нет таймаута/обработки отказов LLM; форма ответа не соответствует контракту | Правила в Context Pack; TRAINING_PR.diff: app/review_service.py:19-22; app/api.py:35-38 |
| Какие системы и команды участвуют? | FastAPI-приложение, компонент ReviewService, внешний LLM | TRAINING_PR.diff |

## Context Pack для первого рабочего сценария

### Факты и правила

- SEC-1: перед отправкой во внешний LLM из diff удаляются токены, пароли и приватные ключи
- API-1: diff длиннее 20 000 символов отклоняется с HTTP 413
- REL-1: внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в контролируемый ответ
- OUT-1: ответ содержит summary, массив risks (≤3, поля file, line, evidence, risk) и массив checks
- SCOPE-1: сервис только советует; он не пишет код и не выполняет действия в GitHub
- QA-1: риск включается в ответ, только если он подтверждён строкой diff или правилом репозитория
- OBS-1: в лог пишутся только request_id, длительность и статус. Содержимое diff и ответа модели не логируется

### Формат входа и результата

- Вход: JSON с полем diff (строка)
- Результат: JSON с полями summary (строка), risks (массив ≤3 элементов с полями file, line, evidence, risk), checks (массив строк)

### Ограничения и запрещённые действия

- Нельзя отправлять секреты во внешний LLM (SEC-1)
- Отклонять запросы с diff > 20 000 символов (API-1)
- Таймаут LLM-вызова 10 секунд; ошибки конвертировать в контролируемый ответ (REL-1)
- Не выполнять изменения в репозитории/PR от имени сервиса (SCOPE-1)

### Хороший пример

```text
app/review_service.py:20 — diff отправляется во внешний LLM без удаления секретов, что нарушает SEC-1.
Проверка: передать diff с тестовым token и убедиться, что в prompt осталось [REDACTED].
```

### Плохой пример

```text
Код можно улучшить. Добавьте тесты и обработку ошибок.
```

### Что пока неизвестно

- Версия Python в целевой среде
- Детальный контракт логирования и трассировки
- Требуемая семантика HTTP-кодов для пограничных ситуаций

## Как использовали AI

- Для чего: Систематизировать контекст, правила и формат результата на основе diff и Master Prompt
- Тип промпта: master prompt
- Строка в [`prompts.md`](prompts.md): P1-02
- Что проверили и исправили сами: Уточнили форматы входа/выхода и ограничения по правилам SEC-1, API-1, REL-1, OUT-1; добавили примеры хорошего и плохого комментариев
